// Rust Library Bindings — SKELETON
// VERSION: v0.5
//
// High-level Rust API for tensor creation, model building, and training.
// Wraps the C API via FFI (arix_tensor.h, arix_arch.h, arix_train.h).

pub mod tensor;
pub mod model;
pub mod train;
