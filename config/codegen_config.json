{
  "version": "0.5.4",
  "patterns": {
    "include_guard": {
      "template": "#ifndef {GUARD}\n#define {GUARD}\n\n{CONTENT}\n\n#endif",
      "example": "SNEPPX_TENSOR_H"
    },
    "function_header": {
      "template": "/**\n * {BRIEF}\n * @param {PARAM_NAME} {PARAM_DESC}\n * @return {RETURN_DESC}\n */",
      "example": "Creates a new tensor with given shape and dtype."
    },
    "test_skeleton": {
      "template": "#include \"test_common.h\"\n#include \"{HEADER}\"\n\nstatic void test_{NAME}_basic(void) {\n    ASSERT(1, \"placeholder\");\n}\n\nint main(void) {\n    run_test(\"{NAME}_basic\", test_{NAME}_basic);\n    RUN_ALL_TESTS();\n}",
      "example": "SNEPPX_tensor"
    },
    "cmake_target": {
      "template": "add_library({TARGET} STATIC\n    ${{CMAKE_CURRENT_SOURCE_DIR}}/{SRC}\n)\ntarget_include_directories({TARGET} PUBLIC ${{CMAKE_SOURCE_DIR}}/include)\ntarget_link_libraries({TARGET} PUBLIC neural_core_kernel)",
      "example": "my_component"
    }
  }
}
